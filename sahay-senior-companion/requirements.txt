# Runtime dependencies for Vercel (it reads requirements.txt from the project root).
# Keep in sync with backend/requirements.txt, which also carries the test dependencies.
fastapi>=0.110.0
pydantic>=2.6.0
httpx>=0.27.0
python-multipart>=0.0.9
